display_name: ai-dispatch
short_description: 通过共享本地运行时把任务派发给 AI coding CLI。
default_prompt: 使用 ai-dispatch 执行一个代码、review 或调研任务。只有返回 JSON 里带 agent_hint 时，才把 user_facing_summary 原样贴进给用户的回复；同一 session 只保留最新一张，不要叠多轮卡片。
